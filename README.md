# react-redux-exchange-widget

Money exchange widget crafted with react and redux

Supported engines:
`node > 8`

Installation:
`yarn`

Start
`yarn start`

Test
`yarn test`

Test coverage
`yarn test -- --coverage`
