# react-redux-exchange-widget

Money exchange widget crafted with react and redux

Supported engines:
`node > 8`
`npm > 5`

Installation:
`npm i`

Start
`npm start`

Test
`npm test`

Test coverage
`npm test -- --coverage`
